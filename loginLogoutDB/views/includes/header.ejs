 <header>
        </h2><a href="/">KZOLM</a></h2>
        <nav>
            <% if (user) { %>
                <a href="/logout">Log Out</a>

            <% } else{ %>
                <a href="/login">Log In</a>
           <% } %>
        </nav>
</header>